# دليل البدء السريع / Quick Start Guide

## ✅ النظام يعمل الآن!

The system is now running!

## 🌐 الوصول للنظام / Access

- **الواجهة الرئيسية**: http://localhost:8000
- **API Documentation**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **Health Check**: http://localhost:8000/health

## 🧪 اختبار سريع / Quick Test

### 1. اختبار تحليل النصوص / Test Text Analysis

افتح المتصفح على: http://localhost:8000/docs

ثم جرب:
- POST `/api/analyze/`
- Body:
```json
{
  "text": "My name is John Doe and my phone is 123-456-7890. Email: test@example.com",
  "apply_policies": false
}
```

### 2. اختبار من المتصفح / Test from Browser

افتح: http://localhost:8000

ستجد واجهة المستخدم مع:
- تبويب تحليل النصوص
- تبويب السياسات
- تبويب التنبيهات
- تبويب المراقبة

## 📝 أمثلة API / API Examples

### تحليل نص
```bash
curl -X POST http://localhost:8000/api/analyze/ \
  -H "Content-Type: application/json" \
  -d '{"text": "Phone: 123-456-7890", "apply_policies": false}'
```

### الحصول على السياسات
```bash
curl http://localhost:8000/api/policies/
```

### الحصول على التنبيهات
```bash
curl http://localhost:8000/api/alerts/
```

### حالة النظام
```bash
curl http://localhost:8000/api/monitoring/status
```

## 🎯 الميزات المتاحة / Available Features

✅ **تحليل النصوص**: اكتشاف البيانات الحساسة (أرقام الهواتف، البريد الإلكتروني، بطاقات الائتمان)
✅ **إدارة السياسات**: إنشاء وتعديل السياسات
✅ **التنبيهات**: عرض وإدارة التنبيهات
✅ **المراقبة**: تقارير وإحصائيات
✅ **قاعدة البيانات**: SQLite (للاختبار) - يمكن الترقية إلى PostgreSQL

## 🔧 الإعدادات الحالية / Current Settings

- **قاعدة البيانات**: SQLite (`backend/test.db`)
- **Presidio**: يستخدم regex patterns (بديل)
- **MyDLP**: معطل (يمكن تفعيله لاحقاً)
- **Port**: 8000

## 📚 الخطوات التالية / Next Steps

1. **استكشف الواجهة**: افتح http://localhost:8000
2. **جرب التحليل**: أدخل نص يحتوي على بيانات حساسة
3. **أنشئ سياسة**: من تبويب السياسات
4. **راجع التنبيهات**: من تبويب التنبيهات
5. **راجع الوثائق**: من http://localhost:8000/docs

## 🎉 النظام جاهز!

The system is ready to use!

