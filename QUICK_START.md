# دليل التشغيل السريع / Quick Start Guide

## 🚀 تشغيل المشروع بسهولة

### Windows

**الطريقة 1: انقر نقراً مزدوجاً**
- انقر نقراً مزدوجاً على `start.bat`
- سيتم فتح نافذة PowerShell/CMD وتشغيل المشروع تلقائياً

**الطريقة 2: من PowerShell**
```powershell
.\start.ps1
```

**الطريقة 3: من CMD**
```cmd
start.bat
```

### Linux / Mac

```bash
# 1. إعطاء صلاحيات التنفيذ
chmod +x start.sh

# 2. تشغيل الـ script
./start.sh
```

## ✨ ما يفعله الـ Script تلقائياً:

1. ✅ **يفحص البيئة الافتراضية** - يبحث عن `venv` في المجلد الرئيسي
2. ✅ **ينشئ البيئة إذا لزم الأمر** - إذا لم تكن موجودة، ينشئها تلقائياً
3. ✅ **يثبت المكتبات** - يثبت جميع المكتبات من `requirements.txt`
4. ✅ **يثبت python-multipart** - مطلوب لرفع الملفات
5. ✅ **يفعل البيئة** - يفعل البيئة الافتراضية تلقائياً
6. ✅ **يشغل السيرفر** - يبدأ uvicorn على المنفذ 8000
7. ✅ **يفتح المتصفح** - يفتح المتصفح تلقائياً على `http://127.0.0.1:8000`

## 📋 المتطلبات الأساسية

قبل التشغيل، تأكد من تثبيت:
- ✅ Python 3.8 أو أحدث
- ✅ pip (عادة يأتي مع Python)

## 🔧 استكشاف الأخطاء

### المشكلة: "Python not found"
**الحل:** تأكد من تثبيت Python وإضافته إلى PATH

### المشكلة: "Permission denied" (Linux/Mac)
**الحل:** 
```bash
chmod +x start.sh
```

### المشكلة: "Cannot activate virtual environment"
**الحل:** 
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

## 🌐 الوصول إلى النظام

بعد التشغيل، النظام سيكون متاحاً على:
- **الواجهة الرئيسية:** http://127.0.0.1:8000
- **API Documentation:** http://127.0.0.1:8000/docs
- **ReDoc:** http://127.0.0.1:8000/redoc

## ⏹️ إيقاف السيرفر

اضغط `Ctrl+C` في نافذة الـ terminal لإيقاف السيرفر.

## 📝 ملاحظات

- السيرفر يعمل في وضع `--reload` (يتحدث تلقائياً عند تغيير الكود)
- البيئة الافتراضية (`venv`) لا يتم رفعها إلى Git (مستثناة في `.gitignore`)
- جميع المكتبات يتم تثبيتها في البيئة الافتراضية فقط

